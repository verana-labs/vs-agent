{
  "name": "@verana-labs/vs-agent-plugin-openid4vc",
  "main": "build/index",
  "types": "build/index",
  "exports": {
    ".": {
      "types": "./build/index.d.ts",
      "require": "./build/index.js",
      "default": "./build/index.js"
    }
  },
  "version": "1.12.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/verana-labs/vs-agent"
  },
  "files": [
    "build"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "description": "OpenID4VC issuer and verifier plugin for VS Agent",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./build",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build",
    "version": "pnpm version --allow-same-version",
    "test": "vitest"
  },
  "dependencies": {
    "@credo-ts/openid4vc": "catalog:",
    "@owf/token-status-list": "0.3.1",
    "@verana-labs/vs-agent-sdk": "workspace:*",
    "express": "^4.18.1"
  },
  "peerDependencies": {
    "@credo-ts/core": "catalog:"
  },
  "devDependencies": {
    "@credo-ts/askar": "catalog:",
    "@credo-ts/node": "catalog:",
    "@openwallet-foundation/askar-nodejs": "^0.6.0"
  }
}
